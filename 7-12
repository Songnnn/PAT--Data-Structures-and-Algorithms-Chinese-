#include<bits/stdc++.h>
using namespace std;

//有点投机取巧
int main()
{
    int n;
    scanf("%d",&n);
    long long A[n];
    for( int i=0 ; i<n ; i++ ) cin>>A[i];
    sort(A,A+n);

    cout<<A[0];
    for( int i=1 ; i<n ; i++ ) cout<<" "<<A[i];

    return 0;
}
